name: Build Dev

on:
  push:
    branches: [ "develop" ]
  pull_request:
    branches: [ "main", "develop" ]
    types: [ labeled, synchronize ]

permissions:
  pull-requests: write
  actions: read
  contents: read

concurrency:
  group: ${{ github.workflow }}-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:

  pr-comment-init:
    if: ${{ github.event.pull_request.head.repo.full_name == github.repository && ((github.event.action == 'labeled' && github.event.label.name == 'build') || (github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'build'))) }}
    runs-on: ubuntu-latest
    steps:
      - name: Post initial build comment
        uses: actions/github-script@v7
        with:
          script: |
            const runId = context.runId;
            const prNumber = context.payload.pull_request.number;
            const { owner, repo } = context.repo;
            const dockerTag = `pr${prNumber}`;

            const body = [
              `<!-- finanze-build-artifacts -->`,
              `## 📦 Build Artifacts`,
              ``,
              `**Workflow run:** [#${runId}](https://github.com/${owner}/${repo}/actions/runs/${runId})`,
              ``,
              `### Desktop Builds`,
              ``,
              `| Platform | Architecture | Type | Size | Link |`,
              `|----------|-------------|------|------|------|`,
              `| 🍎 macOS | ARM64 (Apple Silicon) | \`.dmg\` | — | ⏳ Building... |`,
              `| 🍎 macOS | Intel x64 | \`.dmg\` | — | ⏳ Building... |`,
              `| 🐧 Linux | x64 | \`.AppImage\` | — | ⏳ Building... |`,
              `| 🪟 Windows | x64 | \`.exe\` | — | ⏳ Building... |`,
              ``,
              `### 📱 Android Builds`,
              ``,
              `| Variant | Type | Size | Link |`,
              `|---------|------|------|------|`,
              `| Full | \`.apk\` | — | ⏳ Building... |`,
              `| Full | \`.aab\` | — | ⏳ Building... |`,
              `| Store | \`.apk\` | — | ⏳ Building... |`,
              `| Store | \`.aab\` | — | ⏳ Building... |`,
              ``,
              `### 🐳 Docker Images`,
              ``,
              `<!-- section:docker -->`,
              `⏳ Building on tag \`${dockerTag}\`...`,
              `<!-- /section:docker -->`,
            ].join('\n');

            const marker = '<!-- finanze-build-artifacts -->';
            const { data: comments } = await github.rest.issues.listComments({
              owner,
              repo,
              issue_number: prNumber,
            });
            const existing = comments.find(c => c.body && c.body.includes(marker));

            if (existing) {
              await github.rest.issues.updateComment({
                owner,
                repo,
                comment_id: existing.id,
                body,
              });
            } else {
              await github.rest.issues.createComment({
                owner,
                repo,
                issue_number: prNumber,
                body,
              });
            }


  build-app:
    if: ${{ github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action == 'labeled' && github.event.label.name == 'build') || (github.event_name == 'pull_request' && github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'build')) }}

    strategy:
      matrix:
        os: [ macos-latest, macos-15-intel, ubuntu-latest, windows-latest ]

    runs-on: ${{ matrix.os }}

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          package_json_file: frontend/app/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: frontend/app/.nvmrc

      - name: Setup python (non-macOS)
        if: ${{ !startsWith(matrix.os, 'macos-') }}
        uses: actions/setup-python@v6
        with:
          python-version: 3.14.5

      - name: Setup python (macOS)
        if: ${{ startsWith(matrix.os, 'macos-') }}
        shell: bash
        run: |
          brew update && brew install pyenv pyenv-virtualenv

          export PYENV_ROOT="$HOME/.pyenv"
          export PATH="$PYENV_ROOT/bin:$PATH"
          
          # Initialize pyenv and pyenv-virtualenv for the current shell
          eval "$(pyenv init --path)" # Ensures pyenv command is found via shims/bin
          eval "$(pyenv init -)"      # Sets up shims and other integrations
          eval "$(pyenv virtualenv-init -)" # Defines 'pyenv activate' and other venv functions

          pyenv install --skip-existing 3.14.5
          
          # Create virtualenv if it doesn't exist
          if ! pyenv virtualenvs --bare | grep -q "^finanze$"; then
            pyenv virtualenv 3.14.5 finanze
          else
            echo "Virtualenv 'finanze' already exists."
          fi
          
          # Activate for this script block (confirms setup)
          pyenv activate finanze
          
          # Set for pyenv to use this venv in the current directory context
          pyenv local finanze
          
          # Make pyenv and the virtualenv available to subsequent steps in the job
          echo "$PYENV_ROOT/bin" >> $GITHUB_PATH
          echo "$PYENV_ROOT/shims" >> $GITHUB_PATH
          echo "$PYENV_ROOT/versions/finanze/bin" >> $GITHUB_PATH

      - name: Install CD, packaging & runtime deps
        shell: bash
        run: |
          pip install -r requirements-packaging.txt -r requirements.txt -r requirements-cd.txt

      - name: Setup virtual env (non-Windows)
        if: ${{ matrix.os == 'ubuntu-latest' }}
        shell: bash
        run: |
          python3 -m venv $HOME/venv
          echo "$HOME/venv/bin" >> $GITHUB_PATH

      - name: Compute Dev Version
        id: dev_version
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v bump-my-version &> /dev/null; then
            echo "bump-my-version not found after installation." >&2
            exit 1
          fi
          git fetch --tags --quiet || true
          BASE_VERSION=$(bump-my-version show current_version || echo "0.0.0")
          if [[ $BASE_VERSION =~ ^v ]]; then
            TAG_BASE="$BASE_VERSION"
            BASE_NO_V=${BASE_VERSION#v}
          else
            TAG_BASE="v$BASE_VERSION"
            BASE_NO_V="$BASE_VERSION"
          fi
          LAST_TAG=$(git describe --tags --abbrev=0 --match "v[0-9]*" 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            COUNT=$(git rev-list ${LAST_TAG}..HEAD --count)
          else
            COUNT=$(git rev-list HEAD --count)
          fi
          SHORT_SHA=$(git rev-parse --short HEAD)
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            DEV_VERSION="${BASE_NO_V}-pr${{ github.event.pull_request.number }}-${SHORT_SHA}"
          else
            DEV_VERSION="${BASE_NO_V}-dev${COUNT}-${SHORT_SHA}"
          fi
          echo "Computed dev version: $DEV_VERSION (base: $BASE_NO_V)"
          echo "dev_version=$DEV_VERSION" >> "$GITHUB_OUTPUT"

      - name: Hack bumpversion config with dev version
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        env:
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Injecting DEV_VERSION into .bumpversion.toml (hack)"
          cp .bumpversion.toml .bumpversion.toml.bak
          python3 - <<'PY'
          import os, pathlib, re
          DEV=os.environ['DEV_VERSION']
          path=pathlib.Path('.bumpversion.toml')
          text=path.read_text()
          # Replace all occurrences of {new_version} (with braces) by the actual dev version string
          new_text=text.replace('{new_version}', DEV)
          path.write_text(new_text)
          print('Replaced {new_version} placeholders with', DEV)
          # Show resulting lines referencing replace patterns
          for line in new_text.splitlines():
              if 'replace =' in line:
                  print('CONFIG:', line)
          PY
          echo "Diff preview (first 30 lines):"
          head -n 30 .bumpversion.toml

      - name: Replace project versions with dev version (ephemeral)
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        env:
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Running bump-my-version replace using injected config"
          bump-my-version replace --allow-dirty || { echo "bump-my-version replace failed" >&2; exit 1; }
          echo "Replacement complete. Showing updated versions:"
          grep -E 'version =|"version":' pyproject.toml frontend/app/package.json || true

      - name: Package
        env:
          FINANZE_DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
          GH_TOKEN: ${{ secrets.GH_TOKEN }}
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          CSC_LINK: ${{ startsWith(matrix.os, 'macos-') && secrets.CSC_LINK || '' }}
          CSC_KEY_PASSWORD: ${{ startsWith(matrix.os, 'macos-') && secrets.CSC_KEY_PASSWORD || '' }}
          APPLE_ID: ${{ startsWith(matrix.os, 'macos-') && secrets.APPLE_ID || '' }}
          APPLE_APP_SPECIFIC_PASSWORD: ${{ startsWith(matrix.os, 'macos-') && secrets.APPLE_APP_SPECIFIC_PASSWORD || '' }}
          APPLE_TEAM_ID: ${{ startsWith(matrix.os, 'macos-') && secrets.APPLE_TEAM_ID || '' }}
        shell: bash
        run: |
          echo "Using dev version for artifact naming only: $FINANZE_DEV_VERSION"
          
          # Determine OS and arch parameters
          OS_PARAM=""
          
          if [[ "${{ matrix.os }}" == "macos-latest" ]]; then
            OS_PARAM="--os mac --arch arm64"
          elif [[ "${{ matrix.os }}" == "macos-15-intel" ]]; then
            OS_PARAM="--os mac --arch x64"
          elif [[ "${{ matrix.os }}" == "ubuntu-latest" ]]; then
            OS_PARAM="--os linux"
          elif [[ "${{ matrix.os }}" == "windows-latest" ]]; then
            OS_PARAM="--os win"
          fi
          
          SIGNING_PARAM=""
          if [[ "${{ github.event_name }}" == "pull_request" && "${{ github.event.pull_request.head.repo.full_name }}" != "${{ github.repository }}" ]]; then
            SIGNING_PARAM="--no-sign"
          fi

          python3 package.py --target full $OS_PARAM $SIGNING_PARAM

      - name: Write Dev Version File
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        shell: bash
        run: |
          echo "${{ steps.dev_version.outputs.dev_version }}" > DEV_VERSION

      - name: Upload macOS artifact
        if: startsWith(matrix.os, 'macos')
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-${{ matrix.os }}
          path: dist/*.dmg
          if-no-files-found: warn
          include-hidden-files: false

      - name: Upload Ubuntu artifact
        if: ${{ matrix.os == 'ubuntu-latest' }}
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-${{ matrix.os }}
          path: dist/*.AppImage
          if-no-files-found: warn
          include-hidden-files: false

      - name: Upload Windows artifact
        if: ${{ matrix.os == 'windows-latest' }}
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-${{ matrix.os }}
          path: dist/*.exe
          if-no-files-found: warn
          include-hidden-files: false

      - name: Update PR comment
        if: ${{ always() && github.event_name == 'pull_request' && github.event.pull_request.head.repo.full_name == github.repository }}
        uses: actions/github-script@v7
        with:
          script: |
            const version = '${{ steps.dev_version.outputs.dev_version }}';
            const runId = context.runId;
            const prNumber = context.payload.pull_request.number;
            const { owner, repo } = context.repo;
            const os = '${{ matrix.os }}';
            const jobStatus = '${{ job.status }}';
            const marker = '<!-- finanze-build-artifacts -->';

            const platformInfo = {
              'macos-latest': { icon: '🍎', label: 'macOS', arch: 'ARM64 (Apple Silicon)', type: '.dmg' },
              'macos-15-intel': { icon: '🍎', label: 'macOS', arch: 'Intel x64', type: '.dmg' },
              'ubuntu-latest': { icon: '🐧', label: 'Linux', arch: 'x64', type: '.AppImage' },
              'windows-latest': { icon: '🪟', label: 'Windows', arch: 'x64', type: '.exe' },
            };
            const rowId = {
              'macos-latest': 'ARM64 (Apple Silicon)',
              'macos-15-intel': 'Intel x64',
              'ubuntu-latest': '🐧 Linux',
              'windows-latest': '🪟 Windows',
            }[os];
            const info = platformInfo[os];
            if (!info || !rowId) return;

            const formatSize = (bytes) => {
              const mb = bytes / 1024 / 1024;
              return mb >= 1024 ? `${(mb / 1024).toFixed(2)} GB` : `${mb.toFixed(1)} MB`;
            };

            let newRow;
            if (jobStatus === 'success' && version) {
              const { data: artifacts } = await github.rest.actions.listWorkflowRunArtifacts({ owner, repo, run_id: runId });
              const artifact = artifacts.artifacts.find(a => a.name === `finanze-${version}-${os}`);
              if (artifact) {
                const size = formatSize(artifact.size_in_bytes);
                const url = `https://github.com/${owner}/${repo}/actions/runs/${runId}/artifacts/${artifact.id}`;
                newRow = `| ${info.icon} ${info.label} | ${info.arch} | \`${info.type}\` | ${size} | [Download](${url}) |`;
              } else {
                newRow = `| ${info.icon} ${info.label} | ${info.arch} | \`${info.type}\` | — | ⚠️ No artifact |`;
              }
            } else {
              const status = jobStatus === 'cancelled' ? '⚪ Cancelled' : '❌ Failed';
              newRow = `| ${info.icon} ${info.label} | ${info.arch} | \`${info.type}\` | — | ${status} |`;
            }

            async function doUpdate() {
              const { data: comments } = await github.rest.issues.listComments({ owner, repo, issue_number: prNumber });
              const existing = comments.find(c => c.body && c.body.includes(marker));
              if (!existing) return;
              const lines = existing.body.split('\n');
              const updated = lines.map(line => line.includes(rowId) ? newRow : line);
              const newBody = updated.join('\n');
              if (newBody !== existing.body) {
                await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body: newBody });
              }
            }

            await doUpdate();
            await new Promise(r => setTimeout(r, 2000));
            await doUpdate();


  build-android:
    if: ${{ (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && (github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action == 'labeled' && github.event.label.name == 'build') || (github.event_name == 'pull_request' && github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'build'))) }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Setup pnpm
        uses: pnpm/action-setup@v6
        with:
          package_json_file: frontend/app/package.json

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: frontend/app/.nvmrc

      - name: Setup Java
        uses: actions/setup-java@v5
        with:
          distribution: temurin
          java-version: '21'

      - name: Setup Python
        uses: actions/setup-python@v6
        with:
          python-version: 3.14.5

      - name: Install Python build deps
        run: pip install python-minifier

      - name: Install CD deps
        run: pip install -r requirements-cd.txt

      - name: Setup Go
        uses: actions/setup-go@v6
        with:
          go-version: '1.25'

      - name: Install gomobile
        run: |
          go install golang.org/x/mobile/cmd/gomobile@latest
          go install golang.org/x/mobile/cmd/gobind@latest
          gomobile init

      - name: Setup Android SDK & NDK
        uses: android-actions/setup-android@v4
        with:
          packages: 'ndk;28.0.13004108'

      - name: Install frontend dependencies
        working-directory: frontend/app
        run: pnpm install --frozen-lockfile

      - name: Build native TLS client (Android)
        working-directory: frontend/app
        run: |
          bash native/tlsclient/build.sh --platform android
          bash native/tlsclient/install.sh

      - name: Setup Pyodide & Python wheels
        working-directory: frontend/app
        run: pnpm install:pyodide

      - name: Compute Dev Version
        id: dev_version
        shell: bash
        run: |
          set -euo pipefail
          if ! command -v bump-my-version &> /dev/null; then
            echo "bump-my-version not found after installation." >&2
            exit 1
          fi
          git fetch --tags --quiet || true
          BASE_VERSION=$(bump-my-version show current_version || echo "0.0.0")
          if [[ $BASE_VERSION =~ ^v ]]; then
            TAG_BASE="$BASE_VERSION"
            BASE_NO_V=${BASE_VERSION#v}
          else
            TAG_BASE="v$BASE_VERSION"
            BASE_NO_V="$BASE_VERSION"
          fi
          LAST_TAG=$(git describe --tags --abbrev=0 --match "v[0-9]*" 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            COUNT=$(git rev-list ${LAST_TAG}..HEAD --count)
          else
            COUNT=$(git rev-list HEAD --count)
          fi
          SHORT_SHA=$(git rev-parse --short HEAD)
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            DEV_VERSION="${BASE_NO_V}-pr${{ github.event.pull_request.number }}-${SHORT_SHA}"
          else
            DEV_VERSION="${BASE_NO_V}-dev${COUNT}-${SHORT_SHA}"
          fi
          echo "Computed dev version: $DEV_VERSION (base: $BASE_NO_V)"
          echo "dev_version=$DEV_VERSION" >> "$GITHUB_OUTPUT"

      - name: Hack bumpversion config with dev version
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        env:
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Injecting DEV_VERSION into .bumpversion.toml (hack)"
          cp .bumpversion.toml .bumpversion.toml.bak
          python3 - <<'PY'
          import os, pathlib, re
          DEV=os.environ['DEV_VERSION']
          path=pathlib.Path('.bumpversion.toml')
          text=path.read_text()
          new_text=text.replace('{new_version}', DEV)
          path.write_text(new_text)
          print('Replaced {new_version} placeholders with', DEV)
          for line in new_text.splitlines():
              if 'replace =' in line:
                  print('CONFIG:', line)
          PY
          echo "Diff preview (first 30 lines):"
          head -n 30 .bumpversion.toml

      - name: Replace project versions with dev version (ephemeral)
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        env:
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Running bump-my-version replace using injected config"
          bump-my-version replace --allow-dirty || { echo "bump-my-version replace failed" >&2; exit 1; }
          echo "Replacement complete. Showing updated versions:"
          grep -E 'version =|"version":' pyproject.toml frontend/app/package.json || true

      - name: Decode keystore
        run: echo "${{ secrets.ANDROID_KEYSTORE_BASE64 }}" | base64 -d > frontend/app/android/finanze-release.jks

      - name: Build full variant
        working-directory: frontend/app
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_GOOGLE_WEB_CLIENT_ID: ${{ secrets.VITE_GOOGLE_WEB_CLIENT_ID }}
        run: |
          pnpm build:mobile
          pnpm exec cap sync android

      - name: Build full APK & AAB
        working-directory: frontend/app/android
        env:
          STORE_FILE: ${{ github.workspace }}/frontend/app/android/finanze-release.jks
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: ./gradlew assembleFullRelease bundleFullRelease

      - name: Build store variant
        working-directory: frontend/app
        env:
          VITE_SUPABASE_URL: ${{ secrets.VITE_SUPABASE_URL }}
          VITE_SUPABASE_PUBLISHABLE_KEY: ${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          VITE_GOOGLE_WEB_CLIENT_ID: ${{ secrets.VITE_GOOGLE_WEB_CLIENT_ID }}
        run: |
          pnpm build:mobile:store
          pnpm exec cap sync android

      - name: Build store APK & AAB
        working-directory: frontend/app/android
        env:
          STORE_FILE: ${{ github.workspace }}/frontend/app/android/finanze-release.jks
          STORE_PASSWORD: ${{ secrets.ANDROID_KEYSTORE_PASSWORD }}
          KEY_ALIAS: ${{ secrets.ANDROID_KEY_ALIAS }}
          KEY_PASSWORD: ${{ secrets.ANDROID_KEY_PASSWORD }}
        run: ./gradlew assembleStoreRelease bundleStoreRelease

      - name: Rename outputs
        shell: bash
        run: |
          VERSION=${{ steps.dev_version.outputs.dev_version }}
          mkdir -p dist-android
          cp frontend/app/android/app/build/outputs/apk/full/release/app-full-release.apk dist-android/Finanze-${VERSION}-android-full.apk
          cp frontend/app/android/app/build/outputs/bundle/fullRelease/app-full-release.aab dist-android/Finanze-${VERSION}-android-full.aab
          cp frontend/app/android/app/build/outputs/apk/store/release/app-store-release.apk dist-android/Finanze-${VERSION}-android-store.apk
          cp frontend/app/android/app/build/outputs/bundle/storeRelease/app-store-release.aab dist-android/Finanze-${VERSION}-android-store.aab

      - name: Upload Android full APK
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-android-full-apk
          path: dist-android/Finanze-${{ steps.dev_version.outputs.dev_version }}-android-full.apk
          if-no-files-found: warn

      - name: Upload Android full AAB
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-android-full-aab
          path: dist-android/Finanze-${{ steps.dev_version.outputs.dev_version }}-android-full.aab
          if-no-files-found: warn

      - name: Upload Android store APK
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-android-store-apk
          path: dist-android/Finanze-${{ steps.dev_version.outputs.dev_version }}-android-store.apk
          if-no-files-found: warn

      - name: Upload Android store AAB
        uses: actions/upload-artifact@v6
        with:
          name: finanze-${{ steps.dev_version.outputs.dev_version }}-android-store-aab
          path: dist-android/Finanze-${{ steps.dev_version.outputs.dev_version }}-android-store.aab
          if-no-files-found: warn

      - name: Update PR comment
        if: ${{ always() && github.event_name == 'pull_request' }}
        uses: actions/github-script@v7
        with:
          script: |
            const version = '${{ steps.dev_version.outputs.dev_version }}';
            const runId = context.runId;
            const prNumber = context.payload.pull_request.number;
            const { owner, repo } = context.repo;
            const jobStatus = '${{ job.status }}';
            const marker = '<!-- finanze-build-artifacts -->';

            const formatSize = (bytes) => {
              const mb = bytes / 1024 / 1024;
              return mb >= 1024 ? `${(mb / 1024).toFixed(2)} GB` : `${mb.toFixed(1)} MB`;
            };

            const androidArtifacts = [
              { key: 'android-full-apk', variant: 'Full', type: '.apk' },
              { key: 'android-full-aab', variant: 'Full', type: '.aab' },
              { key: 'android-store-apk', variant: 'Store', type: '.apk' },
              { key: 'android-store-aab', variant: 'Store', type: '.aab' },
            ];

            let artifactList = [];
            if (jobStatus === 'success' && version) {
              const { data: artifacts } = await github.rest.actions.listWorkflowRunArtifacts({ owner, repo, run_id: runId });
              artifactList = artifacts.artifacts;
            }

            const newRows = androidArtifacts.map(({ key, variant, type }) => {
              if (jobStatus === 'success' && version) {
                const artifact = artifactList.find(a => a.name === `finanze-${version}-${key}`);
                if (artifact) {
                  const size = formatSize(artifact.size_in_bytes);
                  const url = `https://github.com/${owner}/${repo}/actions/runs/${runId}/artifacts/${artifact.id}`;
                  return { variant, type, row: `| ${variant} | \`${type}\` | ${size} | [Download](${url}) |` };
                }
                return { variant, type, row: `| ${variant} | \`${type}\` | — | ⚠️ No artifact |` };
              }
              const status = jobStatus === 'cancelled' ? '⚪ Cancelled' : '❌ Failed';
              return { variant, type, row: `| ${variant} | \`${type}\` | — | ${status} |` };
            });

            async function doUpdate() {
              const { data: comments } = await github.rest.issues.listComments({ owner, repo, issue_number: prNumber });
              const existing = comments.find(c => c.body && c.body.includes(marker));
              if (!existing) return;
              const lines = existing.body.split('\n');
              for (const { variant, type, row } of newRows) {
                for (let i = 0; i < lines.length; i++) {
                  if (lines[i].includes(`| ${variant} |`) && lines[i].includes(type)) {
                    lines[i] = row;
                    break;
                  }
                }
              }
              const newBody = lines.join('\n');
              if (newBody !== existing.body) {
                await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body: newBody });
              }
            }

            await doUpdate();
            await new Promise(r => setTimeout(r, 2000));
            await doUpdate();


  build-docker:
    if: ${{ (github.event_name != 'pull_request' || github.event.pull_request.head.repo.full_name == github.repository) && (github.event_name == 'push' || (github.event_name == 'pull_request' && github.event.action == 'labeled' && github.event.label.name == 'build') || (github.event_name == 'pull_request' && github.event.action == 'synchronize' && contains(github.event.pull_request.labels.*.name, 'build'))) }}

    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install CD deps
        shell: bash
        run: |
          pip install -r requirements-cd.txt

      - name: Compute Dev Version
        id: dev_version
        shell: bash
        run: |
          set -euo pipefail
          git fetch --tags --quiet || true
          BASE_VERSION=$(bump-my-version show current_version || echo "0.0.0")
          if [[ $BASE_VERSION =~ ^v ]]; then
            TAG_BASE="$BASE_VERSION"
            BASE_NO_V=${BASE_VERSION#v}
          else
            TAG_BASE="v$BASE_VERSION"
            BASE_NO_V="$BASE_VERSION"
          fi
          LAST_TAG=$(git describe --tags --abbrev=0 --match "v[0-9]*" 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            COUNT=$(git rev-list ${LAST_TAG}..HEAD --count)
          else
            COUNT=$(git rev-list HEAD --count)
          fi
            SHORT_SHA=$(git rev-parse --short HEAD)
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            DEV_VERSION="${BASE_NO_V}-pr${{ github.event.pull_request.number }}-${SHORT_SHA}"
          else
            DEV_VERSION="${BASE_NO_V}-dev${COUNT}-${SHORT_SHA}"
          fi
          echo "Computed dev version for docker: $DEV_VERSION (base: $BASE_NO_V)"
          echo "dev_version=$DEV_VERSION" >> "$GITHUB_OUTPUT"

      - name: Determine Docker Tag
        id: docker_tag
        shell: bash
        run: |
          if [[ "${{ github.event_name }}" == "pull_request" ]]; then
            DOCKER_TAG="pr${{ github.event.pull_request.number }}"
            FRONTEND_TAGS="$DOCKER_TAG"
            SELENIUM_TAGS="$DOCKER_TAG-selenium"
            NOSELENIUM_TAGS="$DOCKER_TAG,$DOCKER_TAG-no-selenium"
          else
            DOCKER_TAG="dev"
            FRONTEND_TAGS="$DOCKER_TAG,next"
            SELENIUM_TAGS="$DOCKER_TAG-selenium,next-selenium"
            NOSELENIUM_TAGS="$DOCKER_TAG,$DOCKER_TAG-no-selenium,next,next-no-selenium"
          fi
          echo "docker_tag=$DOCKER_TAG" >> "$GITHUB_OUTPUT"
          echo "frontend_tags=$FRONTEND_TAGS" >> "$GITHUB_OUTPUT"
          echo "selenium_tags=$SELENIUM_TAGS" >> "$GITHUB_OUTPUT"
          echo "noselenium_tags=$NOSELENIUM_TAGS" >> "$GITHUB_OUTPUT"
          echo "Using Docker tag: $DOCKER_TAG"

      - name: Hack bumpversion config with dev version
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        env:
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Injecting DEV_VERSION into .bumpversion.toml (hack)"
          cp .bumpversion.toml .bumpversion.toml.bak
          python3 - <<'PY'
          import os, pathlib, re
          DEV=os.environ['DEV_VERSION']
          path=pathlib.Path('.bumpversion.toml')
          text=path.read_text()
          # Replace all occurrences of {new_version} (with braces) by the actual dev version string
          new_text=text.replace('{new_version}', DEV)
          path.write_text(new_text)
          print('Replaced {new_version} placeholders with', DEV)
          # Show resulting lines referencing replace patterns
          for line in new_text.splitlines():
              if 'replace =' in line:
                  print('CONFIG:', line)
          PY
          echo "Diff preview (first 30 lines):"
          head -n 30 .bumpversion.toml

      - name: Replace project versions with dev version (ephemeral)
        if: ${{ steps.dev_version.outputs.dev_version != '' }}
        env:
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          echo "Running bump-my-version replace using injected config"
          bump-my-version replace --allow-dirty || { echo "bump-my-version replace failed" >&2; exit 1; }
          echo "Replacement complete. Showing updated versions:"
          grep -E 'version =|"version":' pyproject.toml frontend/app/package.json || true

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v2

      - name: Publish dev frontend image
        uses: elgohr/Publish-Docker-Github-Action@v5
        with:
          name: ${{ secrets.DOCKER_USERNAME }}/finanze-frontend
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          default_branch: develop
          workdir: frontend
          platforms: linux/amd64,linux/arm64/v8
          buildargs: VITE_SUPABASE_URL=${{ secrets.VITE_SUPABASE_URL }},VITE_SUPABASE_PUBLISHABLE_KEY=${{ secrets.VITE_SUPABASE_PUBLISHABLE_KEY }}
          tags: ${{ steps.docker_tag.outputs.frontend_tags }}

      - name: Publish selenium image
        uses: elgohr/Publish-Docker-Github-Action@v5
        with:
          name: ${{ secrets.DOCKER_USERNAME }}/finanze
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          default_branch: develop
          platforms: linux/amd64,linux/arm64/v8
          buildargs: SELENIUM_SUPPORT=true
          tags: ${{ steps.docker_tag.outputs.selenium_tags }}

      - name: Publish no-selenium image
        uses: elgohr/Publish-Docker-Github-Action@v5
        with:
          name: ${{ secrets.DOCKER_USERNAME }}/finanze
          username: ${{ secrets.DOCKER_USERNAME }}
          password: ${{ secrets.DOCKER_PASSWORD }}
          default_branch: develop
          platforms: linux/amd64,linux/arm64/v8
          buildargs: SELENIUM_SUPPORT=false
          tags: ${{ steps.docker_tag.outputs.noselenium_tags }}

      - name: Update PR comment
        if: ${{ always() && github.event_name == 'pull_request' }}
        uses: actions/github-script@v7
        with:
          script: |
            const runId = context.runId;
            const prNumber = context.payload.pull_request.number;
            const { owner, repo } = context.repo;
            const jobStatus = '${{ job.status }}';
            const marker = '<!-- finanze-build-artifacts -->';
            const dockerTag = `pr${prNumber}`;

            let dockerContent;
            if (jobStatus === 'success') {
              dockerContent = [
                `| Image | Tag | Pull Command |`,
                `|-------|-----|-------------|`,
                `| \`${{ secrets.DOCKER_USERNAME }}/finanze\` | \`${dockerTag}\` | \`docker pull ${{ secrets.DOCKER_USERNAME }}/finanze:${dockerTag}\` |`,
                `| \`${{ secrets.DOCKER_USERNAME }}/finanze\` | \`${dockerTag}-selenium\` | \`docker pull ${{ secrets.DOCKER_USERNAME }}/finanze:${dockerTag}-selenium\` |`,
                `| \`${{ secrets.DOCKER_USERNAME }}/finanze-frontend\` | \`${dockerTag}\` | \`docker pull ${{ secrets.DOCKER_USERNAME }}/finanze-frontend:${dockerTag}\` |`,
              ].join('\n');
            } else if (jobStatus === 'cancelled') {
              dockerContent = `⚪ Docker build was cancelled.`;
            } else {
              dockerContent = `❌ Docker build failed — check the [workflow run](https://github.com/${owner}/${repo}/actions/runs/${runId}) for details.`;
            }

            async function doUpdate() {
              const { data: comments } = await github.rest.issues.listComments({ owner, repo, issue_number: prNumber });
              const existing = comments.find(c => c.body && c.body.includes(marker));
              if (!existing) return;
              const body = existing.body;
              const startMarker = '<!-- section:docker -->';
              const endMarker = '<!-- /section:docker -->';
              const startIdx = body.indexOf(startMarker);
              const endIdx = body.indexOf(endMarker);
              if (startIdx === -1 || endIdx === -1) return;
              const before = body.substring(0, startIdx + startMarker.length);
              const after = body.substring(endIdx);
              const newBody = before + '\n' + dockerContent + '\n' + after;
              if (newBody !== existing.body) {
                await github.rest.issues.updateComment({ owner, repo, comment_id: existing.id, body: newBody });
              }
            }

            await doUpdate();
            await new Promise(r => setTimeout(r, 2000));
            await doUpdate();


  publish-next:
    if: ${{ github.event_name == 'push' && github.ref == 'refs/heads/develop' }}
    needs: [ build-app, build-android, build-docker ]
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Checkout Code
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Install CD deps
        shell: bash
        run: pip install -r requirements-cd.txt

      - name: Compute Dev Version
        id: dev_version
        shell: bash
        run: |
          set -euo pipefail
          git fetch --tags --quiet || true
          BASE_VERSION=$(bump-my-version show current_version || echo "0.0.0")
          if [[ $BASE_VERSION =~ ^v ]]; then
            BASE_NO_V=${BASE_VERSION#v}
          else
            BASE_NO_V="$BASE_VERSION"
          fi
          LAST_TAG=$(git describe --tags --abbrev=0 --match "v[0-9]*" 2>/dev/null || echo "")
          if [ -n "$LAST_TAG" ]; then
            COUNT=$(git rev-list ${LAST_TAG}..HEAD --count)
          else
            COUNT=$(git rev-list HEAD --count)
          fi
          SHORT_SHA=$(git rev-parse --short HEAD)
          DEV_VERSION="${BASE_NO_V}-dev${COUNT}-${SHORT_SHA}"
          echo "Computed dev version: $DEV_VERSION"
          echo "dev_version=$DEV_VERSION" >> "$GITHUB_OUTPUT"

      - name: Download all build artifacts
        uses: actions/download-artifact@v6
        with:
          path: artifacts
          merge-multiple: true

      - name: Collect release assets
        shell: bash
        run: |
          set -euo pipefail
          mkdir -p dist
          find artifacts -type f \
            \( -name '*.dmg' -o -name '*.AppImage' -o -name '*.exe' \
               -o -name '*.apk' -o -name '*.aab' \) \
            -exec cp -f {} dist/ \;
          echo "Collected assets:"
          ls -la dist

      - name: Move next tag to current commit
        shell: bash
        run: |
          set -euo pipefail
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git tag -f next "$GITHUB_SHA"
          git push -f origin next

      - name: Create or update next pre-release
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          DEV_VERSION: ${{ steps.dev_version.outputs.dev_version }}
        shell: bash
        run: |
          set -euo pipefail
          TITLE="Next (develop build $DEV_VERSION)"
          if gh release view next >/dev/null 2>&1; then
            echo "Updating existing 'next' pre-release"
            gh release edit next --prerelease --latest=false --title "$TITLE" --target develop
            echo "Deleting previous assets"
            for asset in $(gh release view next --json assets --jq '.assets[].name'); do
              echo "Removing $asset"
              gh release delete-asset next "$asset" -y || true
            done
          else
            echo "Creating 'next' pre-release"
            gh release create next \
              --prerelease \
              --latest=false \
              --target develop \
              --title "$TITLE" \
              --notes "Rolling pre-release automatically built from the latest \`develop\` commit. Not intended for production use."
          fi
          gh release upload next dist/* --clobber

      - name: Update next release description with build table
        uses: actions/github-script@v7
        with:
          script: |
            const { owner, repo } = context.repo;
            const tag = 'next';
            const version = '${{ steps.dev_version.outputs.dev_version }}';

            const { data: release } = await github.rest.repos.getReleaseByTag({ owner, repo, tag });
            const { data: assets } = await github.rest.repos.listReleaseAssets({
              owner, repo, release_id: release.id,
            });

            const formatSize = (bytes) => {
              const mb = bytes / 1024 / 1024;
              return mb >= 1024 ? `${(mb / 1024).toFixed(2)} GB` : `${mb.toFixed(1)} MB`;
            };

            const desktopBuilds = [
              { icon: '🍎', label: 'macOS', arch: 'ARM64 (Apple Silicon)', pattern: '-mac-arm64.dmg' },
              { icon: '🍎', label: 'macOS', arch: 'Intel x64', pattern: '-mac-x64.dmg' },
              { icon: '🐧', label: 'Linux', arch: 'x64', pattern: '-linux-x86_64.AppImage' },
              { icon: '🪟', label: 'Windows', arch: 'x64', pattern: '-win-x64.exe' },
            ];

            const buildAppResult = '${{ needs.build-app.result }}';
            let desktopRows = '';
            for (const build of desktopBuilds) {
              const asset = assets.find(a => a.name.endsWith(build.pattern));
              if (asset) {
                const size = formatSize(asset.size);
                desktopRows += `| ${build.icon} ${build.label} | ${build.arch} | ${size} | [Download](${asset.browser_download_url}) |\n`;
              } else {
                const status = buildAppResult === 'failure' ? '❌ Failed' : buildAppResult === 'cancelled' ? '⚪ Cancelled' : '⚠️ Not found';
                desktopRows += `| ${build.icon} ${build.label} | ${build.arch} | — | ${status} |\n`;
              }
            }

            const androidBuilds = [
              { type: '.apk', pattern: `-android-full.apk` },
              { type: '.aab', pattern: `-android-full.aab` },
            ];

            const buildAndroidResult = '${{ needs.build-android.result }}';
            let androidRows = '';
            for (const build of androidBuilds) {
              const asset = assets.find(a => a.name.endsWith(build.pattern));
              if (asset) {
                const size = formatSize(asset.size);
                androidRows += `| \`${build.type}\` | ${size} | [Download](${asset.browser_download_url}) |\n`;
              } else {
                const status = buildAndroidResult === 'failure' ? '❌ Failed' : buildAndroidResult === 'cancelled' ? '⚪ Cancelled' : '⚠️ Not found';
                androidRows += `| \`${build.type}\` | — | ${status} |\n`;
              }
            }

            const dockerResult = '${{ needs.build-docker.result }}';
            let dockerSection;
            if (dockerResult === 'success') {
              dockerSection = [
                `| Image | Tag | Pull Command |`,
                `|-------|-----|-------------|`,
                `| \`${{ secrets.DOCKER_USERNAME }}/finanze\` | \`next\` | \`docker pull ${{ secrets.DOCKER_USERNAME }}/finanze:next\` |`,
                `| \`${{ secrets.DOCKER_USERNAME }}/finanze\` | \`next-selenium\` | \`docker pull ${{ secrets.DOCKER_USERNAME }}/finanze:next-selenium\` |`,
                `| \`${{ secrets.DOCKER_USERNAME }}/finanze-frontend\` | \`next\` | \`docker pull ${{ secrets.DOCKER_USERNAME }}/finanze-frontend:next\` |`,
              ].join('\n');
            } else if (dockerResult === 'failure') {
              dockerSection = `❌ Docker build failed.`;
            } else if (dockerResult === 'cancelled') {
              dockerSection = `⚪ Docker build was cancelled.`;
            } else {
              dockerSection = `⚠️ Docker build status unknown.`;
            }

            const table = [
              ``,
              `## 📦 Build Assets`,
              ``,
              `_Rolling pre-release built from \`develop\` — version \`${version}\`._`,
              ``,
              `### Desktop`,
              ``,
              `| Platform | Architecture | Size | Link |`,
              `|----------|-------------|------|------|`,
              desktopRows.trim(),
              ``,
              `### 📱 Android`,
              ``,
              `| Type | Size | Link |`,
              `|------|------|------|`,
              androidRows.trim(),
              ``,
              `### 🐳 Docker Images`,
              ``,
              dockerSection,
            ].join('\n');

            const existingBody = release.body || '';
            const buildMarker = '## 📦 Build Assets';
            let newBody;
            if (existingBody.includes(buildMarker)) {
              newBody = existingBody.substring(0, existingBody.indexOf(buildMarker)).trimEnd() + '\n' + table;
            } else {
              newBody = existingBody.trimEnd() + '\n' + table;
            }

            await github.rest.repos.updateRelease({
              owner, repo, release_id: release.id,
              body: newBody,
            });
